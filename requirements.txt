Flask>=2.3.2,<3.0
gunicorn>=21.2.0,<22.0
Werkzeug>=3.0.0,<4.0
Jinja2>=3.1.2,<4.0
itsdangerous>=2.1.2,<3.0
MarkupSafe>=2.1.3,<3.0

# Optional but recommended
Flask-WTF>=1.2.1,<2.0

# Used by /tutor integration (app degrades gracefully if missing)
requests==2.32.3

# Stripe (app degrades gracefully if missing; required to accept payments)
stripe>=9.0.0,<11.0
